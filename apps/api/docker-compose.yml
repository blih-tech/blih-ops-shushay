services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: blih
      POSTGRES_PASSWORD: blih
      POSTGRES_DB: blih_dev
    ports:
      - "5432:5432"
    volumes:
      - blih_pg_data:/var/lib/postgresql/data

volumes:
  blih_pg_data:
